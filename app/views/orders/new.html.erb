
<% if @company %>
<h1>New Order for <%= @company.name %> </h1>
 <% else %>
 <h1> New Order </h1>
<% end %>

<% if @order.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(order.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
      <% @order.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
   
<%= form_for([@company, @order]) do |c| %>
   <% if !@company %>
    <%= c.label :company %>
    <%= c.collection_select :company_id, Company.all, :id,  :company_name %><br><br>
    <% else %>
    <%= c.hidden_field :order_id%>
    <% end %>
    <%= c.label :Description %>
    <%= c.text_field :description %> <br><br>
    <%= c.label :quantity %>
    <%= c.number_field :quantity  %><br><br>
    
    <%= c.submit %>
<% end %>

